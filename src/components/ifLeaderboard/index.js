const IfCardHasLeaderboard = ({ getleaderboardData, getLeaderboardData }) => {
  // if (getleaderboardData.id === getLeaderboardData.product_id) {
  //   return <div>Hello</div>;
  // }
  console.log(getleaderboardData, getLeaderboardData);
};

export default IfCardHasLeaderboard;
